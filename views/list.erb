<div id="list">
    <% if !@urls.blank? %>
        <table>
            <tr>
                <th>shrtn</th>
                <th>clicks</th>
                <th>destination</th>
            </tr>
            <% for links in @urls %>
            <tr>
                <td>
                    <a href="<%= link.shortcode %>">
                        <%= link.shortcode %>
                    </a>
                </td>

            </tr>
            <% end %>
        </table>
    <% else %>
        <p>Nothing avaible at the moment!</p>
    <% end %>
</div>